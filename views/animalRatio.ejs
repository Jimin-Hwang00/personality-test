<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>동물 비율</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            padding: 20px;
        }
        h1 {
            text-align: center;
        }
        table {
            width: 80%;
            margin: 0 auto;
            border-collapse: collapse;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: center;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
    <h1>동물 비율</h1>
    <table class="table table-striped table-bordered">
        <thead class="thead-light">
            <tr>
                <th>이름</th>
                <th>비율</th>
            </tr>
        </thead>
        <tbody>
            <% animalRatio.forEach(animal => { %>
                <tr>
                    <td><%= animal.name %></td>
                    <td><%= animal.ratio.toFixed(2) %> %</td> <!-- 소수점 2자리까지 표시 -->
                </tr>
            <% }); %>
        </tbody>
    </table>
</body>
</html>
